# Default registry (public npm)
registry=https://registry.npmjs.org/

# Always save exact versions instead of version ranges
save-exact=true

# Enable detailed logs for debugging
loglevel=info

# Ensure SSL is enforced
strict-ssl=true
